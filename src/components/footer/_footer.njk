{% macro greatFooter(isInternational, currentYear, footerLinks) %}
    <footer id="footer"
        dir="ltr"
        data-ga-section="footer"
        class="great-ds-footer">
        <div class="great-ds-footer__container {% if isInternational %}great-ds-footer__container--international{% endif %}">
            {% if isInternational %}
                <div alt="Expand your business in the UK, start of service"  class="great-ds-footer__great-logo-container">
                    {% include "./_footer-great-logo.njk" %}
                </div>
            {% endif %}
            <div class="great-ds-container">
                <ul class="great-ds-footer__link-list {% if isInternational %}great-ds-footer__link-list--international{% endif %}">
                    {% for link in footerLinks %}     
                        <li class="great-ds-footer__link-list-item {% if isInternational %}great-ds-footer__link-list-item--international{% endif %}">
                            <a href="{{ link.href }}"
                            title="{{ link.title }}"
                            {% if link.target %} target="{{ link.target }}" {% endif %}
                            {% if link.rel %} rel="{{ link.rel }}" {% endif %} >
                            {{ link.text }}
                            </a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
        <div class="great-ds-footer__copyright-container">
            <div class="great-ds-container">
                {% include "./_footer-dbt-logo.njk" %}
                <p class="great-ds-footer__copyright{% if isInternational %} great-ds-footer__copyright--international {% endif %}">© Crown copyright {{ currentYear }}. All rights reserved.</p>
            </div>
    </div>
    </footer>
{% endmacro %}